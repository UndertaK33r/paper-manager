# 工具调用规则

- `sandbox_permissions` 和 `justification` 是可选字段，普通调用必须省略。
- 遇到 `not strictly wider` 报错时，保持原命令不变，去掉这两个字段后重试一次。
- 不得切换权限档位反复尝试，也不得重复发送相同的失败调用。
- 只有实际操作被沙箱拒绝且明确允许提权时，才携带这两个字段原样重试一次。
- 会话禁止审批时，不申请提权；实际访问被拒绝后停止该操作并说明。
