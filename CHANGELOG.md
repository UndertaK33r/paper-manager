# 更新日志

## v1.3.0（2026-09-10）

### PDF 阅读与标注（本版重点）

- **原始版面渲染**：内置 pdf.js 阅读器（离线可用），按论文原版排版渲染；
  加载失败才回退浏览器原生阅读器（该模式无法标注，界面有说明）
- **划段高亮**：鼠标选中 PDF 文字 → 弹出四色高亮（黄/绿/蓝/粉）；
  点已有高亮可改颜色、写备注、删除
- **文字批注**：点「加批注」后点击页面任意位置放置批注框，
  可直接输入文字、按住抓手拖动、删除
- **划词翻译**：选中文字点「翻译」或按 `Alt+T` → 译文浮层（Markdown 渲染），
  可一键把译文存为页面批注；译文自动收集到译文面板
- **标注持久化**：以「页码 + 归一化坐标」单独存储，**不改动原 PDF 文件**；
  单栏/双栏论文都精确，缩放、全宽、全屏下自动对齐，重新打开自动加载
- 详情页「标注」列表区分高亮/批注、显示页码，点击定位并闪烁，
  可一键按 Markdown 插入笔记

### 笔记

- 支持 Markdown 渲染预览（自研 `md-mini`：HTML 转义 + URL 白名单，不引入依赖）

### 移除

- 纯文本重排阅读视图（`SplitReading` 及其接口）：重排会丢失版面结构，
  双栏 PDF 的文字抽取顺序本身也可能错乱，不适合承载标注

### 工程

- 笔记时间戳精度由毫秒提高到微秒，修复同一毫秒内连续编辑被判定为未变化的问题
- 删除死代码与不可达功能；README、使用说明同步更新

---

## v1.2.0（2026-09-09）

- 回收站：删除进回收站，可恢复、可彻底删除、可清空
- 一键备份：导出包含数据库快照与 PDF 的 zip
- 笔记支持 Markdown 渲染预览
- 访问密码：支持 HTTP Basic / Bearer / `?token=` 三种方式，静态页面不拦截

## v1.1.0（2026-09-09）

- 引用图谱（OpenAlex）
- AI 元数据提取与摘要
- 标签、分类、合集管理

## v1.0.1（2026-09-08）

- 修复 PDF 替换后残留旧文件的问题

## v1.0.0（2026-09-08）

- 首个版本：论文导入（PDF 元数据与全文提取）、列表与详情、笔记、
  BibTeX 导出、搜索、状态与收藏
